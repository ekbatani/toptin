using Toptin.Api.Data.Interfaces.Base;
using Toptin.Api.Models;

namespace Toptin.Api.Data.Interfaces
{
    public interface IBrand : IInterfaceBase<Brand>
    {
        //Task<IEnumerable<Brand>> GetAllOwnersAsync();
        //Task<Brand> GetOwnerByIdAsync(Guid ownerId);
        ////Task<OwnerExtended> GetOwnerWithDetailsAsync(Guid ownerId);
        //Task CreateOwnerAsync(Brand owner);
        //Task UpdateOwnerAsync(Brand dbOwner, Brand owner);
        //Task DeleteOwnerAsync(Brand owner);
    }
}
